{
  "buildCommand": "corepack enable pnpm && pnpm install && pnpm build",
  "installCommand": "corepack enable pnpm && pnpm install --frozen-lockfile",
  "outputDirectory": "artifacts/vmp-store/.next"
}
