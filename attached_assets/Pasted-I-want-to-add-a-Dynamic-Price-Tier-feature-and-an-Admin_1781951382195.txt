I want to add a Dynamic Price Tier feature and an Admin Input Section to my e-commerce website so that I can easily update any product's image link, retail price, and wholesale tiered prices directly from the UI. Please implement the following architecture:

1. Data Structure Update for Products:
   - Update the product schema/object structure to support:
     * id: (Unique product identifier)
     * name: (Product Name)
     * image_url: (Direct image link input)
     * retail_price: (Price for single/small quantities)
     * wholesale_tier_1: { min_qty: X, price: Y } (e.g., 50-100 units)
     * wholesale_tier_2: { min_qty: A, price: B } (e.g., 100+ units)
     * stock_status: (In Stock / Out of Stock)

2. Secure Admin/Control Section UI:
   - Create a hidden or password-protected toggle section at the bottom of the page (or a dedicated route/modal) titled "Veer Mahadev Product Control".
   - Inside this section, provide a clean HTML Form or dynamic table where I can select an existing product or add a new one.
   - The form fields must include: Product Name, Direct Image URL Link, Retail Price, Wholesale Tier 1 Price, and Wholesale Tier 2 Price.
   - Include a "Save & Update Catalog" button that pushes this data into the local database/state instantly.

3. Frontend Price-Tier Display:
   - Update the Amazon-style product cards in the #productGrid to display this tiered pricing beautifully.
   - Example UI display inside the card:
     * Retail Price: ₹150 / unit
     * Bulk (50+ units): ₹120 / unit
     * Factory Rate (100+ units): ₹95 / unit
   - When a user changes the quantity in their cart, the cart logic must automatically apply the correct tiered price based on the total quantity ordered.

4. Local Persistence:
   - Ensure that whenever I add a new image link or price via the Admin section, the data is saved using localStorage or the existing database so the changes do not disappear when the page reloads.

Please generate the frontend input interface and link it with the product generation logic safely without interrupting the existing WhatsApp setup.